## prerequisites:
 - npm, bower
 - mysql

## install:

	root# npm install
	root# bower install jsgrid

## import:

	root# cd import
	root# mysql -u root -p <$database_name> <bulk.sql

## run:

	root# PORT=<$port> npm start
